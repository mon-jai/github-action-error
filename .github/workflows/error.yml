
name: Error
on: [push, workflow_dispatch]

jobs:
  error:
    runs-on: windows-latest
    concurrency:
      group: ${{ matrix.arguments }}
    strategy:
      matrix:
        arguments: ["foo", "bar"]
